.DS_Store
.bsp/
.metals/
.scala-build/
project/target/
target/
modules/*/target/

# Local secrets — loaded by sbt-dotenv plugin; never commit
.env

# Editor / IDE / agent state — never commit
.idea/
.claude/
.agents/
