# Installation

dspy4s is pre-release and is not yet on Maven Central. Build it from source with JDK 21 and sbt:

```bash
git clone https://github.com/jpablo/dspy4s.git
cd dspy4s
sbt test
```

The build pins Scala 3.8.4.
