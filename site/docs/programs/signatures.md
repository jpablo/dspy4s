# Signatures

`Signature[I, O]` connects Scala types to a language-model record protocol. `Signature.derived[I, O]` derives shapes
from case classes. The signatures module also supports named tuples, `Spec` declarations, and programmatic layouts.

Program syntax uses typed values. Dynamic records appear at adapter, dataset, and persistence boundaries.
